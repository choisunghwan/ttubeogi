# 뚜버기 (Ttubeogi) — 개발 스펙

> Claude Code 전달용 문서. 이 문서 + 데모 파일(`ttubeogi.jsx`)을 함께 주고 "이 스펙대로, 디자인·캐릭터·동선 느낌은 데모 그대로 만들어줘"라고 지시하면 됩니다.
> 앱 이름: **뚜버기** — 지도 위를 뚜벅뚜벅 걷는 캐릭터가 곧 앱의 마스코트이자 이름. "뚜버기가 오늘 동선을 안내해줄게" 식으로 캐릭터를 매개로 앱 전체에 성격을 부여.
> **캐릭터 디자인은 데모의 SVG 뚜버기(크림색 마시멜로 몸통 + 주황 배낭 + 여행 깃발)를 그대로 사용.**

---

## 1. 제품 개요

여러 명이 **같이 어디 가는 일정을 실시간으로 함께 만들고 관리하는** 웹앱.
여행만이 아니라 **데이트·약속·나들이 등 "같이 다니는 모든 일정"**을 다룬다.
(여행은 그중 여러 날짜리 큰 이벤트일 뿐이고, 데이트·약속은 하루짜리 가벼운 일정이다.)

기존 여행 계획 앱들은 한 명이 일정을 다 짠 뒤 공유하는 "정적 계획표"이고, 여행 때만 쓴다.
뚜버기는 다르다 — 일정 하나가 **링크 하나**가 되고, 친구·연인·가족이 그 링크로 들어와
**각자 일정을 직접 추가·수정하고, 그 변경이 서로의 화면에 즉시 반영된다.**
단톡방에서 "여기 어때?" "거긴 별로" 하던 과정을 앱 하나로 흡수한다.
데이트·약속까지 다루므로 여행 앱의 약점(가끔만 열림)을 넘어 **자주 쓰는 앱**이 되는 것이 목표다.

- **핵심 차별점**: 실시간 공동 편집 (구글 독스처럼 여럿이 같은 일정을 동시에 만짐)
- **일정 종류**: 여행 · 데이트 · 약속 (같은 목록에 종류 뱃지로 구분, 본질은 동일)
- **보조 기능**: 후보 장소 투표·좋아요 (의견이 갈릴 때만 사용)
- **여행 기록 지도**: 다녀온 곳이 한국/세계 지도에 발자국으로 쌓임 (많이 갈수록 진해지고, 단골은 뚜버기가 앉아있음). 여행이 끝나도 남는 재미.
- **타겟**: 친구·연인·가족 (커플·자주 만나는 친구 그룹). 첫 실사용 목표: 9월 상하이 여행
- **AI**: 1차 범위에서는 넣지 않는다 (순수 협업 기능으로 승부). 이후 확장 여지만 남긴다.
- **국내·해외 모두 지원**: 지도는 전 세계 표시 가능, 검색만 지역별로 자동 전환(아래 3장 참고)

---

## 2. 핵심 사용자 시나리오

1. 방장이 여행을 만든다 (제목·기간·인원).
2. 생성된 **공유 링크**를 일행에게 보낸다.
3. 일행은 **로그인 없이 이름만 입력**하고 참여한다.
4. 각자 가고 싶은 곳을 일정에 추가한다 → 다른 사람 화면에 즉시 뜬다.
5. 시간·순서를 드래그로 바꾸면 지도의 동선·경로가 실시간으로 다시 그려진다.
6. 의견이 갈리는 후보는 좋아요·투표로 정한다.
7. 완성된 일정은 지도에서 **다음/이전** 버튼으로 동선을 따라가며 확인한다.
8. 여행 당일에도 같은 링크로 열어 오늘의 동선을 확인한다.

---

## 3. 기술 스택 (전부 무료 티어 목표)

| 역할 | 기술 | 비고 |
|---|---|---|
| 호스팅(정적) | Cloudflare Workers Assets / Pages | React 빌드 결과물 배포 |
| 백엔드(서버로직) | Cloudflare Workers (JavaScript) | API·요청 처리. 별도 서버 불필요(서버리스) |
| 메인 DB | **Cloudflare D1 (SQLite/SQL)** | 일정·멤버·항목·발자국 기록 영구 저장. SQL 그대로 사용 |
| 실시간 동기화 | Cloudflare Durable Objects + WebSocket | 일정 "방" 하나당 연결 관리, 실시간 편집 상태 |
| (선택) 캐시/설정 | Cloudflare KV | 1차엔 필수 아님 |
| 지도 엔진 | Leaflet | 무료 오픈소스 |
| 지도 타일(그림) | CARTO Voyager | 무료, API 키 불필요 |
| 지도 데이터 | OpenStreetMap | CARTO가 렌더링 |
| 장소 검색(지오코딩) | **하이브리드: 국내=카카오맵 / 해외=Nominatim** (보조: 지도 링크 붙여넣기) | 지역에 따라 검색 엔진 자동 전환 |
| 프론트 | **React** | 데모(`ttubeogi.jsx`)와 동일 구성 |

> **Cloudflare 하나로 전부 해결 (정적 + 동적 둘 다):**
> Cloudflare는 정적 호스팅만 하는 게 아니라 서버 코드 실행·DB·실시간까지 다 된다.
> - 화면(React) → Workers Assets/Pages (정적)
> - 서버 로직(Controller/Service 역할) → Workers (JS)
> - DB(관계형, SQL) → **D1** ← 성환의 DB2 자리에 해당, SQL 그대로 씀
> - 실시간 통신 → Durable Objects + WebSocket
> 별도 서버 임대·DB 구매 없이 전부 무료 티어 안에서 동작. 서버리스라 안 쓰면 0원.

> **검색 전략: 국내·해외 둘 다 지원 (지역별 자동 전환)**
> 카카오맵은 국내 검색 정확도가 최고지만 **해외 장소는 검색이 안 된다.**
> 따라서 여행 지역에 따라 검색 엔진을 자동으로 바꾼다:
> - **국내 → 카카오맵 로컬 API** ("대구 혁신도시" 같은 것도 정확)
> - **해외 → Nominatim(무료 OSM)** ("Eiffel Tower", "The Bund" 등 유명지 잘 잡힘)
> - **둘 다 못 잡으면 → 사용자가 지도 링크(구글맵 등)를 붙여넣어 좌표 확보**
>
> **카카오 비용·절차 걱정 해소됨 (2026년 7월 기준):**
> - 로컬 API 무료 쿼터 = 최대 10만 호출/일, 계정 통합 월 300만. 개인·소규모 여행 앱은 사실상 무료
> - 무료량 초과해도 **자동 과금이 아니라 429 에러로 막힘** (돈 빠질 걱정 없음)
> - 2026년 7월 21일부터 절차 간소화 — 관리자 심사 없이 앱 관리 페이지에서 활성화만 하면 즉시 사용
> - 무료 쿼터는 개발자 계정의 '첫 번째로 활성화한 앱'에 제공 → 이 여행 앱을 그 첫 앱으로
>
> **⚠️ 중국(상하이) 특이사항 — 첫 실사용이 9월 상하이 여행이므로 중요:**
> - 상하이·베이징 등 대도시는 OSM 데이터가 좋은 편이라 Nominatim으로 유명 관광지·역은 잘 잡힘
> - 단, 중국은 지도 규제(GCJ-02 좌표계) 때문에 CARTO/OSM 배경지도 위에서 핀이 실제 위치보다 수십 미터 어긋나 보일 수 있음 → 여행용으론 감수 가능한 수준
> - Nominatim이 중국 지명의 국가/행정구역을 가끔 잘못 표기하는 버그 있음(좌표는 대체로 정상)
> - **대응**: 중국 검색 시 "위치가 살짝 다르면 지도 링크를 붙여넣어 주세요" 안내 노출. 애매하면 바이두/구글맵 링크로 보완
> - **지도 타일(그림)**: 1차는 무료 CARTO 유지

---

## 4. 데이터 모델

> 여행·데이트·약속을 별도 타입으로 나누지 않고 하나의 **Plan**으로 통합. `kind`로만 구분.

```
Plan (일정 — 여행/데이트/약속 공통)
  - id           : 고유 ID (URL에 사용, 예: shanghai-a3f9)
  - kind         : 여행 | 데이트 | 약속
  - title        : 제목 (예: "상하이 여행", "홍대 데이트")
  - startDate    : 시작일
  - endDate      : 종료일 (하루짜리면 startDate와 동일)
  - region       : 대표 지역/도시 (발자국 지도 집계용, 예: "부산", "상하이")
  - members[]    : 참여자 목록 (이름, 색상, 접속 상태)
  - days[]       : Day 배열 (데이트·약속은 보통 1일)
  - status       : upcoming | past

Day (하루)
  - date         : 날짜
  - city         : 도시/지역
  - items[]      : Item 배열 (시간순 정렬)

Item (일정 항목)
  - id
  - type         : 명소 | 식당 | 카페 | 쇼핑 | 숙소 | 이동 | 항공 | 기타
  - time         : 시간
  - name         : 이름 (지도 라벨에 표시)
  - query        : 지도 검색어/주소 (좌표 변환용)
  - lat, lng     : 좌표 (검색으로 채워짐)
  - mapLink      : 구글/카카오맵 링크 (선택, 있으면 좌표 대신 바로 연결)
  - move         : 이동수단 (도보 | 지하철 | 버스 | 트램 | 택시 등)
  - detail       : 종류별 상세 (항공=편명/시간/터미널, 숙소=체크인아웃/예약번호 등)
  - votes[]      : 좋아요/투표한 멤버 목록
  - itemStatus   : 후보(candidate) | 확정(confirmed)
  - createdBy    : 추가한 멤버

VisitRecord (발자국 기록 — 나의 지도용)
  - region       : 지역 (예: "부산")
  - visits       : 방문 횟수 (완료된 Plan 수로 집계)
  - lastVisit    : 마지막 방문일
  - cities[]     : 가본 세부 장소 목록
  - lat, lng     : 지도 표시 좌표
  ※ Plan이 past가 될 때마다 해당 region의 visits +1 로 자동 집계
```

발자국 표시 레벨: 1~2회=작은/연한 발자국, 3~4회=크고 진한 발자국+횟수 배지,
5회 이상=뚜버기가 그 자리에 앉아있음(단골). (데모 로직 참고)

---

## 5. 화면 명세

> 데모(`ttubeogi.jsx`)에 홈·여행상세·나의지도 3화면과 캐릭터가 구현되어 있음. 데모를 시각적 기준으로 삼을 것.

### 5-0. 홈 (앱 첫 화면) ★데모 구현됨
- 상단: 뚜버기 로고 + 프로필
- 인사말 "어디, 누구랑 갈까요?"
- **다가오는 일정**: 여행·데이트·약속이 한 목록에 섞여 시간순. 각 카드에 종류 뱃지·D-day·멤버 아바타·장소 수
- **지난 일정**: 완료된 것들 (→ 발자국 지도에 반영됨)
- 하단: **＋ 새 일정 만들기** 버튼
- 카드 탭 → 해당 일정 상세로

### 5-0b. 나의 지도 (여행 기록) ★데모 구현됨
- 한국/세계 지도에 다녀온 지역이 발자국으로 쌓임 (레벨별 표시)
- 상단 요약: 총 여행 수 · 다녀온 지역 수 · 최애 여행지
- 지역 탭 → 방문 횟수 + 마지막 방문일 + 가본 곳 목록
- 하단 탭바로 홈 ↔ 나의 지도 전환

### 5-1. 일정 만들기
- 종류(여행/데이트/약속), 제목, 기간(시작~종료일), 인원 입력 → 생성 → 공유 링크 발급

### 5-2. 참여 (초대받은 사람)
- 링크 열면 이름 입력만으로 참여 (로그인 없음)
- 참여 시 랜덤/선택 색상 배정 (누가 무엇을 추가했는지 색으로 구분)

### 5-3. 상단 헤더 (일정 상세)
- 제목, 기간, "3박 4일", 인원 수
- D-DAY 배지 (당일이면 "오늘 출발! D-DAY")
- 현재 접속 중인 멤버 표시 (실시간)

### 5-4. 탭: 오버뷰(지도) / 일정(리스트)
- **날짜별 칩 네비게이션** (여행은 여러 날, 데이트·약속은 보통 1일)

### 5-5. 오버뷰 (지도) 탭 ★데모 구현됨
- Leaflet + CARTO 지도 (데모는 SVG로 흉내냄 → 실제는 Leaflet)
- 종류별 색상 핀 마커 + 번호
- 시간 라벨 (예: "13:30 알파마 점심")
- 이동 경로 선 (지나온 길 회색 / 남은 길 주황 점선)
- **걷는 뚜버기 캐릭터**: 좌표 사이를 보간(interpolation)하며 이동
  - 데모의 SVG 뚜버기 그대로 사용, 걸을 때 통통 튀고 다리 움직이고 방향 전환
  - 실제 Leaflet에선 커스텀 DivIcon으로 얹기
  - 1차: 직선 경로 보간 / 2차: 도로 따라(OSRM 또는 카카오 길찾기)
- **다음 / 이전 / 처음** 버튼으로 동선을 하나씩 따라가기
- 오늘의 진행도 표시 (예: 7/7)

### 5-6. 일정 (리스트) 탭
- 날짜별 일정 카드 리스트 (종류별 아이콘·색)
- **드래그로 순서/시간 변경** → 지도 경로 실시간 갱신
- 각 카드에 좋아요·투표 수, 추가한 사람 표시
- 항공편 등 종류별 정보 카드 형태로 표시

### 5-7. 일정 추가 모달
- 종류 선택 (8종 버튼)
- 시간, 날짜(드롭다운)
- 이름 (필수)
- 지도 검색어/주소 (비우면 이름으로 검색) → 카카오 API로 좌표 자동 조회
- 구글맵 링크 직접 넣기 (선택)
- 이동수단 (도보/지하철/버스/트램 버튼 + 직접 입력)
- 저장 → 실시간으로 모든 참여자 화면에 반영

---

## 6. 실시간 동기화 규칙

- 여행 방(Trip id) 하나 = Durable Object 하나
- 참여자는 WebSocket으로 그 방에 연결
- 누군가 일정을 추가/수정/삭제/이동하면 → 방 안 모든 참여자에게 즉시 브로드캐스트
- 접속/이탈 시 멤버 목록 갱신 (누가 지금 보고 있는지)
- **충돌 처리 (1차)**: "마지막 저장이 이김" (last-write-wins). 정교한 병합은 2차로 미룸
- (선택) 누가 지금 어떤 항목을 편집 중인지 표시

---

## 7. 구현 우선순위

**1차 (MVP — 이번 목표, 9월 상하이 여행 전까지)**
1. 홈 화면 (일정 목록: 여행·데이트·약속 통합, 다가오는/지난) — 데모 참고
2. 일정 생성 + 공유 링크 + 이름만으로 참여
3. 일정 CRUD (추가/수정/삭제), 일정 추가 모달
4. 좌표 조회 → 지도 핀 표시 (국내=카카오 / 해외=Nominatim 자동 전환 + 지도 링크 붙여넣기 보조)
5. 실시간 동기화 (여럿이 같이 편집, 즉시 반영) — Durable Objects + WebSocket
6. 오버뷰 지도(Leaflet) + 경로 선 + 걷는 뚜버기 + 다음/이전 네비
7. 나의 지도 (발자국 기록) — 완료된 일정이 지도에 쌓임

**2차**
- 후보 투표·좋아요로 확정
- 드래그 순서 변경
- 도로 따라 경로(OSRM/카카오 길찾기)
- 종류별 상세 정보 카드 (항공 편명 등)

**3차 (여지만)**
- AI 코스 추천/정리
- 일정 후 사진·기록 앨범
- 지도 타일 카카오 통일 검토, 중국 좌표계(GCJ-02) 정밀 보정

---

## 8. 디자인 방향

- 기존 J플래너보다 **더 세련되고 완성도 높은** 느낌 (카피처럼 보이지 않게)
- **캐릭터 뚜버기가 핵심 정체성** — 데모의 SVG 캐릭터 그대로 사용
- 색감: 주황(#e8863a) + 크림(#fdf6e9) 톤. 데모의 팔레트 그대로
- 실시간 협업이라는 정체성이 드러나는 UI (멤버 색상, 접속 표시, 실시간 반영 모션)
- 모바일 우선 (max-width 440px 기준)
- 폰트: Pretendard 계열
- **데모(`ttubeogi.jsx`)를 그대로 디자인 기준으로 삼을 것.** 색·간격·캐릭터·카드 스타일 모두 데모 참고.
